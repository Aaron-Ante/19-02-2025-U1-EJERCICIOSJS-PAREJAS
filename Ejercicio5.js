//Escribe una función llamada saludar(nombre) que reciba un nombre y devuelva un mensaje de saludo,
// por ejemplo: "Hola, Juan".


function saludar(nombre) {  //funcion
    return `Hola, ${nombre}`; // Devuelve un saludo con el nombre proporcionado
}

console.log(saludar("Juan")); // Llama a la función y muestra el saludo en la consola

