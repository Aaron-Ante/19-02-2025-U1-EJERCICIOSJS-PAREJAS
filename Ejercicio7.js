// Dado un arreglo let numeros = [2, 4, 6, 8, 10], usa forEach para imprimir cada número en la consola.
let numeros = [2, 4, 6, 8, 10]; //Array con numeros

numeros.forEach(element => { // Itera sobre cada número del array
    console.log(element); // Imprime el número en la consola
});