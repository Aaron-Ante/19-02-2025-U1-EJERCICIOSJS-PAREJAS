// Dado let palabras = ["banana", "manzana", "pera", "uva"], usa .sort() para ordenarlas alfabéticamente.


let palabras = ["bananas", "manzanas", "pera", "uva"]; // Arreglo con las palabras a ordenar

// Ordena el arreglo alfabéticamente
console.log(palabras.sort()); // Muestra el arreglo ordenado alfabéticamente